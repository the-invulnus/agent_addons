# CI Deploy Skill

一个用于部署应用到CI系统的Claude技能，支持单应用和多应用部署，并提供实时状态监控。支持 Windows、macOS (Intel/Apple Silicon) 和 Linux。

## 触发条件

当用户请求发布或部署应用时触发，例如：
- "把应用kafka-transfer发布一下"
- "部署kafka-transfer"
- "把应用kafka-transfer,app1,app2发布一下"
- "发布一下kafka-transfer"
- "部署一下kafka-transfer"

## 功能说明

此技能会调用ci-deploy工具来执行应用的发布和部署操作，包括：
- 环境验证
- 应用信息查询
- 部署请求发送
- 实时状态监控（最长等待10分钟）

## 部署流程

部署过程分为4个步骤：

1. **验证环境** - 验证EnvID对应的环境是否存在
2. **查询应用信息** - 根据应用名称查询应用ID和部署模式
3. **执行部署** - 向CI系统发送部署请求
4. **监控状态** - 轮询部署状态直到完成或超时

## 使用方式

### macOS / Linux
```bash
~/.claude/skills/ci-deploy/scripts/ci-deploy -apps "kafka-transfer" -config "CI.local.md"
```

### Windows
```powershell
$env:USERPROFILE\.claude\skills\ci-deploy\scripts\ci-deploy.exe -apps "kafka-transfer" -config "CI.local.md"
```

## 前置条件

### 1. 环境变量配置（必需）

AccessKey需要配置在系统环境变量中：

| 环境变量名 | 说明 | 获取方式 |
|-----------|------|---------|
| CI_ACCESS_KEY | 用户访问密钥 | 从CI系统个人设置页面获取 |


### 2. 项目配置文件

需要在当前项目目录下创建`CI.local.md`配置文件，包含以下信息：
- VersionID: 版本ID（数字，从CI系统获取）
- EnvID: 环境ID（数字，从CI系统获取）
- SubjectID: 项目ID（数字，从CI系统获取）

**注意：AccessKey已移至环境变量CI_ACCESS_KEY中，不再需要在配置文件中配置。**

## 配置示例

在项目目录创建`CI.local.md`文件：

```markdown
# CI Deploy Configuration

## 基本信息
- VersionID: 1308        # 版本ID
- EnvID: 412             # 环境ID
- SubjectID: 62          # 项目ID
- Repository: aipaas   # 镜像仓库

## 获取方式

### VersionID
1. 登录CI系统
2. 进入应用详情页
3. 在URL或页面中找到VersionID

### EnvID
1. 在CI系统中查看环境列表
2. 找到目标环境对应的ID
3. 开发环境通常 EnvID: 412

### SubjectID
1. 在CI系统中查看项目详情
2. 找到项目对应的SubjectID

### Repository
1. 在CI系统重查看镜像仓库

### AccessKey (环境变量配置)
1. 登录CI系统
2. 进入个人设置页面
3. 生成或查看AccessKey
4. 配置到系统环境变量 CI_ACCESS_KEY
```

## 部署状态

部署过程中会显示以下状态：
- ⏳ 未部署 - 等待部署开始
- 🔄 部署中 - 正在执行部署
- ✅ 部署成功 - 部署完成
- ❌ 部署失败 - 部署出错

## 部署结果示例

成功部署示例：
```
开始部署流程...
应用: kafka-transfer
版本ID: 1308, 环境ID: 412, 项目ID: 62

>>> 步骤1: 验证环境...
环境名称: 【新一体机开发环境】 (ID: 412)
环境验证通过

>>> 步骤2: 查询应用信息...
找到应用: kafka-transfer (ID: 3132, 模式: build_deploy)

>>> 步骤3: 执行部署...
部署请求已发送，共 1 个应用

>>> 步骤4: 监控部署状态...
[⏳] kafka-transfer: 未部署
[🔄] kafka-transfer: 部署中
[✅] kafka-transfer: 部署成功

🎉 所有应用部署完成!
```

## 重要说明

- AccessKey从环境变量CI_ACCESS_KEY读取，不在项目配置文件中配置
- 配置文件CI.local.md需要用户提前在当前项目目录下准备好
- 如果配置信息不完整，需要引导用户补充配置
- 部署监控最长等待10分钟，超时后会停止轮询
- 支持 Windows、macOS (Intel/Apple Silicon) 和 Linux 操作系统
